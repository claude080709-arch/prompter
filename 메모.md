# Prompter — 메모 (절대 잊으면 안 되는 것들)

## 현재 진행 상황
- 2026-03-24: 디자인 전면 개선 완료 + 추가 버그 수정 완료
- 로컬은 최신, Vercel은 아직 구버전 (push 필요)

## 발견된 버그 & 수정 상태
### ✅ 수정됨: 한국어 해석 섹션 한자 혼입 버그
- 증상: `prompt_ko`에 중국 한자(悠 등)가 섞여 출력
- 원인: `isKorean()` 체크 통과해도 한자가 남아있는 경우 미처리
- 수정: 메인 경로 + fallback 경로 모두 `stripHanja(promptKo)` 최종 후처리 추가
- 파일: app.html line 945, 954

### ✅ 수정됨: 색상 통일
- 두 파일 모두 `--accent: #C4962A` 통일 완료

## 기술적 제약
- `/api/groq` 백엔드 API는 수정 불가 (Vercel 서버리스)
- 프론트엔드만 수정 가능
- 15초 타임아웃 존재

## 디자인 개선 사항 (모두 완료)
1. ✅ 히어로 섹션에 앱 미리보기 mockup 추가
2. ✅ CTA 아래 신뢰 지표 ("완전 무료 · 회원가입 없음") 추가
3. ✅ chip 선택 시 active 상태 CSS 추가
4. ✅ 리뷰 카드에 인용부호 스타일 추가 (::before, 72px 금색 따옴표)
5. ✅ 전문가 카드 선택 시 glow 효과 강화

## 실수 방지
- JavaScript 로직 건드리지 말 것 (reroll, copyText, goBack 등)
- AbortController 로직 건드리지 말 것
- XSS sanitize 코드 건드리지 말 것
- `stripHanja()` 함수 유지할 것 (카드 + promptKo 모두 적용)

## 다음 할 일
- [ ] app.html GitHub push
- [ ] index.html GitHub push
- [ ] Vercel 배포 확인 (push 후 자동 배포)
